import { MapView, ReactLogo, SearchBar } from "../components"

export const HomeScreen = () => {
    return (
        <>
            <MapView />
            <ReactLogo />
            <SearchBar />
        </>
    )
}